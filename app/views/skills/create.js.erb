<% if @skill.errors.any? %>
	$("#new_skill").modal("show");
	$("#error_explanation").remove();
	$("#new_skill .modal-content").append("<%= j render 'shared/error_messages', object: @skill %>");
<% else %>
	$("#new_skill").modal("hide");
	$("#skills-col").prepend("<%= j render @skill %>");
<% end %>