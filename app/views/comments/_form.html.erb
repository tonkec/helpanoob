<%= form_for([@post, @post.comments.build]) do |f| %>
  <%= f.text_area :content, class: "orange-input input-bordered input-full input-padding", placeholder: "Say something smart..." %>
  <%= f.submit "Comment", class: "btn-orange" %>
<% end %>
